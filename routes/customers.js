const express = require("express");
const router = express.Router();
const Joi = require("joi");

router.get("/", (req, res)=>{

    
})

router.post("/", (req, res)=>{});

router.put("/:id", (req, res) => {});

